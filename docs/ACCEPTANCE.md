# LumaCapture 0.1.3 验收记录

日期：2026-09-09
环境：macOS 26.6.2，Apple Silicon arm64，Xcode 26.6 / SDK 26.5

## 自动化结果

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| arm64 核心行为 | 通过 | 9/9：坐标、像素、历史、损坏文件、命名碰撞 |
| x86_64 交叉编译 | 通过 | 生成 Mach-O 64-bit x86_64 测试程序并验证架构 |
| Universal 主程序 | 通过 | `lipo` 验证 arm64 + x86_64 |
| 最低系统 | 通过 | 两个架构的 `LC_BUILD_VERSION minos` 均为 15.0 |
| 包与签名 | 通过 | Info.plist、图标、运行时依赖、严格 codesign 验证 |
| 编辑渲染 | 通过 | 合成图像方向、裁剪、标注、马赛克不透明像素 |
| 图像导出 | 通过 | 500 × 160 PNG/JPEG 写入并通过 ImageIO 回读 |
| OCR 合成图检查 | 通过 | 在完整系统构建环境识别出合成英文/数字；受限沙箱内会被 Vision 拒绝，脚本明确标记为跳过 |
| 4K 编辑性能基准 | 通过 | 3840 × 2160、80 个标注、10,000 个涂鸦点：中位渲染 12.5 ms，p95 12.8 ms；PNG 80.5 ms，JPEG 30.3 ms |
| 编辑器增强自检 | 通过 | 旋转与输出缩放尺寸、真实马赛克降采样像素、编辑导出回读均通过 |
| 真实 ScreenCaptureKit | 权限受限 | 自动检查不触发用户 TCC 授权，不宣称通过 |

## 交互验收

| 场景 | 结果 | 观察 |
| --- | --- | --- |
| 应用启动与控制台 | 通过 | 原生窗口正常显示；无权限时禁用捕获并展示恢复入口 |
| 导入合成 PNG | 通过 | 打开 500 × 160 图像，进入完整编辑器 |
| 工具与状态 | 通过 | 八种工具、颜色、线宽、缩放及导出入口可访问 |
| 工作台模式 | 通过 | 截图、录屏、导入图片同列；默认截图；截图与录屏共享区域范围 |
| 菜单栏模式 | 通过 | 应用固定为 accessory，不创建 Dock 图标；区域/全屏截图与录屏、导入、最近素材直接可用 |
| 编辑增强 | 通过 | 默认无工具、马赛克、圆角矩形、空白文字输入、自定义颜色、二维码、Base64、贴图、旋转缩放入口可用 |
| 箭头标注 | 通过 | 画布拖动后显示“已添加箭头”，撤销变为可用 |
| 撤销/重做 | 通过 | 撤销后重做可用且状态更新 |
| OCR | 通过 | 正常应用会话识别合成图像为 `LUMA 2026` |
| 置顶贴图 | 通过 | 创建可缩放的 floating panel，并带可访问性说明 |
| 实际截图/录屏 | 未执行 | 当前 TCC 屏幕录制权限未授予；更改系统隐私设置须由用户决定 |

## 安装包验收

| 产物 | 结果 |
| --- | --- |
| Universal ZIP | 已生成；解压后再次验证包结构、双架构和签名 |
| Universal DMG | 已生成；`hdiutil verify` 校验有效 |
| SHA-256 | 已生成；本地 `shasum -c` 通过 |

## 分发边界

- 当前机器没有 Developer ID Application 签名身份。产物采用 ad-hoc 签名，未提交 Apple 公证。
- x86_64 已交叉编译并检查 Mach-O 和最低系统版本；当前机器没有可用 Rosetta/Intel 执行环境，未声称 Intel 实机运行通过。
- 真实系统声音和麦克风内容需要用户授权后人工确认；录像实现使用 ScreenCaptureKit `SCRecordingOutput`，完成回调后才把文件加入历史。

## GitHub 状态

GitHub CLI 已认证账号 `danielgzd`。源码、文档、CI、Universal 2 安装包和校验文件发布到公开仓库 `danielgzd/LumaCapture`，代码采用标准 MIT License。代码变更通过 CI 后自动提升补丁版本并发布 Release；大版本可手动指定。
